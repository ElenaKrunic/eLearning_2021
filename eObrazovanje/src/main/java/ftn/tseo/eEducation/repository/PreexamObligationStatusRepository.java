package ftn.tseo.eEducation.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import ftn.tseo.eEducation.model.PreexamObligationStatus;

public interface PreexamObligationStatusRepository extends JpaRepository<PreexamObligationStatus, Long>{

}
